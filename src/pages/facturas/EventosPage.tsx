import { useState } from "react"; import { useQuery } from "@tanstack/react-query"; import { Plus } from "lucide-react"; import RegistrarContingenciaModal from "../../features/facturas/components/RegistrarContingenciaModal"; import { EstadoBadge,SiatEmpty,SiatError,SiatLoading,SiatPageHeader } from "../../features/facturas/components/SiatShared"; import { listarEventosSiat } from "../../features/facturas/services/siat.service"; import "../../features/facturas/styles/siat.css";
const d=(v:string|null)=>v?new Intl.DateTimeFormat("es-BO",{dateStyle:"short",timeStyle:"short"}).format(new Date(v)):"Abierto";
export default function EventosPage(){const q=useQuery({queryKey:["siat-eventos"],queryFn:listarEventosSiat});const [modal,setModal]=useState(false);const data=q.data??[];return <div className="siat-page"><SiatPageHeader title="Gestión de eventos" description="Ventanas de contingencia y relación entre documentos y paquetes." action={<button className="siat-primary-button" onClick={()=>setModal(true)}><Plus/>Crear evento</button>}/><section className="siat-metrics"><article><small>Eventos activos</small><strong>{data.filter(x=>!x.fechaFin).length}</strong></article><article><small>Paquetes</small><strong>{data.reduce((a,x)=>a+x.cantidadPaquetes,0)}</strong></article><article><small>Facturas asociadas</small><strong>{data.reduce((a,x)=>a+x.cantidadFacturas,0)}</strong></article><article><small>Eventos registrados</small><strong>{data.length}</strong></article></section><section className="siat-card">{q.isLoading?<SiatLoading/>:q.error?<SiatError message={q.error.message}/>:data.length===0?<SiatEmpty title="Sin eventos" description="Aún no se registraron eventos significativos."/>:<div className="siat-table-wrap"><table className="siat-table"><thead><tr><th>Sucursal / P.V.</th><th>Facturas</th><th>Vigencia</th><th>Descripción</th><th>Paquetes</th><th>Recepcionados</th><th>Validados</th><th>Estado</th></tr></thead><tbody>{data.map(x=><tr key={x.id}><td>{x.sucursal} / {x.puntoVenta}</td><td>{x.cantidadFacturas}</td><td>{d(x.fechaInicio)}<br/>{d(x.fechaFin)}</td><td>{x.descripcion??x.tipoEventoCodigo??"—"}</td><td>{x.cantidadPaquetes}</td><td>{x.paquetesRecepcionados}</td><td>{x.paquetesValidados}</td><td><EstadoBadge estado={x.estado}/></td></tr>)}</tbody></table></div>}</section>{modal&&<RegistrarContingenciaModal onClose={()=>setModal(false)}/>}</div>}
